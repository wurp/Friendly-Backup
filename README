See https://github.com/wurp/Friendly-Backup/wiki

Build jars with ...

Copy jars and contents of dist/generic to your working directory
Edit var/BackupConfig.properties and set the values to those of your friends

Once you've done so, you can start up the backup process with run.sh or run.cmd

See src/main/java/com/geekcommune/friendlybackup/main/FBNodeApp.java for the main method -
that's the class we start to run a Friendly Backup client.

